# CS 180 Project Journal

A Next.js portfolio for Maixin Zhang's computational photography work.

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000`. Run `npm run build` for a production verification.

Pushes to `main` deploy automatically to GitHub Pages through `.github/workflows/deploy-pages.yml`.
